## Core Unix commands

Most unix commands have super short names, which makes them quick to type but 
annoying to learn. Major file system related commands include:

pwd: Print working directory
ls: List files and directories
cd: Change Directory
mkdir: make a new directory
rm: remove files and directories (delete)
cp: copy files or dirs (source > destination)
mv: move files or dirs (basically re-name)
nano: A wee text editor (very basic but always available)

## AWS EC2 Instance
Connect to my instance with:

ssh -i "bimm143_ella.pem" ubuntu@ec2-34-214-236-118.us-west-2.compute.amazonaws.com

